# [Feature / Requirement Title]

## Summary
One or two sentences describing the feature and its goal.

## User problem / context
Who is affected, what problem they have, and how we know (e.g. research, feedback). Aim to "see the user's expression."

## Requirements
List each requirement with a stable hierarchical ID: `REQ-{AREA}-{NN}` (`AREA` = short uppercase feature code, `NN` = two digits per area). Use these IDs in Jira stories (Maps to: REQ-AUTH-01, …).

| ID     | Requirement description |
|--------|--------------------------|
| REQ-AUTH-01 | …                        |
| REQ-AUTH-02 | …                        |
| REQ-AUTH-03 | …                        |

(Or use a bullet list with **REQ-AUTH-01:**, **REQ-AUTH-02:**, etc.)

## Out of scope
What we are explicitly not doing for this feature.

## NFRs (if any)
Non-functional requirements that apply to this feature (e.g. performance, accessibility, security). Reference in stories where applicable.

## Open questions (if any)
Objective, to-the-point questions for legal, compliance, policy, or other stakeholders. Only include questions that genuinely need stakeholder input or could block implementation. Do not pad.

---
*This file defines the Confluence requirement page format. The **publish_confluence** agent fills it from the signed-off research only and uses hierarchical requirement IDs (`REQ-{AREA}-{NN}`, e.g. REQ-AUTH-01) for traceability. Replace the structure with your team's format as needed.*
