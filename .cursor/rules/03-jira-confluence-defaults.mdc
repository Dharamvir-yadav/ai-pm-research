---
description: Where to find Jira and Confluence defaults for MCP
alwaysApply: true
---

# Jira & Confluence defaults (MCP)

When using Jira or Confluence MCP:

- **Defaults**: Use `.cursor/defaults.md` for Confluence space key/ID, default parent pages, Jira project key, and any other listed defaults.
- **Overview**: See `AGENTS.md` for workflow and high-level MCP usage (e.g. Epic, search limits). Update `.cursor/defaults.md` when you onboard (set real project key, space, parent pages).
